# T5 状态报告

- 任务: T5 — 渲染
- 状态: done
- commit: 329bd8a

## 改动文件
- `src/render/mod.rs`(新建)—— 模块入口,`require` 必填字段助手,re-export `dotenv` / `langchain`。
- `src/render/dotenv.rs`(新建)—— `.env` 片段渲染 + 4 测试。
- `src/render/langchain.rs`(新建)—— LangChain `ChatOpenAI` 片段渲染 + 3 测试。
- `src/main.rs`(改)—— `mod render`(allow dead_code/unused_imports,由 T6 接线)。

## 实现摘要
- **返回 `Secret`(`Zeroizing<String>`)**:产物含明文 key(S5 要求),这是 key 唯一合法出现在输出的地方。用 `push_str` 直接拼进一个受控缓冲,明文只此一处落点,打印/复制后清零;不留非清零中间副本(延续 secret 模块卫生)。
- **dotenv**:变量名 `env_prefix` + 固定后缀(`_API_KEY`/`_BASE_URL`/`_MODEL`/`_EMBEDDING_MODEL`)。缺 chat 省 `_MODEL`、缺 embedding 省 `_EMBEDDING_MODEL`(S5"缺某角色对应行省略")。
- **langchain**:统一 `ChatOpenAI`;有 chat 才出 `model=` 行;只用 base_url + chat(S5 langchain 片段不含 embedding)。
- **缺 MUST 字段报错**:`env_prefix`/`base_url` 缺失 → 错误**指明 provider + 字段名**(spec S3 line 74),错误不含 key。
- 借用细节:拼装闭包放进块作用域,先析构再 `move out`,避免可变借用冲突。

## 自测
```
cargo build  → Finished,无 warning
cargo test   → 54 passed; 1 ignored; 0 failed(T5 新增 7)
cargo clippy → Finished,无 error/warning
```
测试中 key 一律用占位串 `sk-TEST`,无真实 key。

## 对照 DoD
- [x] 对 openrouter 样例输出与 S5 完全一致 —— `dotenv::openrouter_matches_s5_exactly` / `langchain::openrouter_matches_s5_exactly`,byte 级断言两种片段。
- [x] 无 embedding 时省略对应行 —— `omits_embedding_line_when_absent`(deepseek,无 `_EMBEDDING_MODEL`);另含 `omits_model_*` 验证缺 chat 也省略。

## 偏离/疑问
- 渲染函数签名加了 `id: &str` 参数(design 示例为 `render::dotenv(provider, key)`):为满足 spec S3"缺字段须指明 provider 名"的错误要求。属示例签名的合理细化。
- chat 角色按 S5"缺某角色对应行省略"的通用读法处理(缺 chat → dotenv 省 `_MODEL`、langchain 省 `model=` 行);openrouter 样例有 chat,不受影响。若希望缺 chat 时报错而非省略,请指出。
