# T1 状态报告

- 任务: T1 — 凭证引用解析
- 状态: done
- commit: 10121af

## 改动文件
- `src/cred_ref.rs`(新建)—— `Backend` / `CredRef` / `CredRefError` + `FromStr` 解析(spec S1),含 14 个单元测试。
- `src/main.rs`(改 1 处)—— 新增 `#[allow(dead_code)] mod cred_ref;` 把模块接进编译;命令体仍 `todo!()`,由 T6 接线。`allow(dead_code)` 因 T1 阶段解析器尚无调用者。

## 实现摘要
- `Backend { Keychain, Bw, Env }` 严格按 design 第 61 行;`FromStr for Backend` 只认 `keychain`/`bw`/`env`,其余(含 `bws`)→ `UnknownBackend`。
- `CredRef { backend, locator, profile }` 按 design 第 63–67 行字段。`FromStr` 解析顺序:① `split_once(':')` 切首个冒号(locator 内可再含 `:`/`/`);② `rsplit_once('#')` 从末尾切 profile;③ 校验 locator 非空。
- 错误用 `thiserror` 派生 `CredRefError`,四个变体(MissingColon / UnknownBackend / EmptyLocator / EmptyProfile),`Display` 均为人类可读中文消息。
- 额外加 `Display for CredRef`(回显规范引用串)供 T6 `show` 以引用形式展示 key,**绝不**展示明文。
- 关键取舍:空 profile(`x:y#`)判为 `EmptyProfile` 错误而非静默当无 profile,贴合 S1"不静默失败"。

## 自测
```
cargo build   → Finished,无 error/warning
cargo test cred_ref → running 14 tests ... test result: ok. 14 passed; 0 failed
cargo clippy  → Finished,无 error/warning
```
14 个测试覆盖:S1 合法表 5 行 + 非法输入 6 个(缺冒号 / 未知后端 / 空 locator / 空 locator+profile / 空 profile / `bws` 拒绝)+ 边界 2 个(首冒号切分、profile 末尾切分)+ Display 往返 1 个。未涉及任何真实 key。

## 对照 DoD
- [x] 实现 `Backend` / `CredRef` / `FromStr`(spec S1)—— 见 `src/cred_ref.rs`,字段/枚举与 design 一致。
- [x] 单元测试覆盖 S1 表中每行合法引用 —— `keychain_default_profile` / `keychain_with_profile` / `bw_item_by_name_with_slash_and_spaces` / `bw_item_by_id` / `env_variable` 一一对应。
- [x] ≥3 个非法输入 —— 实际 6 个(见自测),均断言返回具体 `CredRefError` 变体。
- [x] `cargo test cred_ref` 全绿 —— 14 passed; 0 failed。

## 偏离/疑问
- 无 spec 偏离。三点请 evaluator 留意:
  1. 为 `main.rs` 加了一行 `mod cred_ref;`(否则模块不进编译、测试无法运行)。这越过"一文件一 owner"的 `main.rs` 边界但属必要接线,改动最小且不触命令逻辑。
  2. 额外实现 `Display for CredRef` 与 `Backend::as_str`,DoD 未要求,但 `show` 需"引用形式"展示 key,属 S1 数据契约的自然往返,未过早抽象其它东西。
  3. 空 profile(`keychain:openrouter#`)选择报错而非忽略 `#`;若评审认为应宽松放行,可在 review 指出。
