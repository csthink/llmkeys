# T0 状态报告

- 任务: T0 — 项目骨架
- 状态: done

## 改动文件
- `Cargo.toml`(新建)—— cargo 项目元信息 + 按 design D1 钉主版本的依赖。
- `src/main.rs`(新建)—— clap derive 入口,全子命令注册,命令体 `todo!()`。
- `rust-toolchain.toml`(新建)—— 固定 stable 通道 + clippy/rustfmt。
- `.gitignore`(已存在,未改动)—— 已含 `.env` / `.env.*` / `*.log` / `/target/`,满足 DoD,故不重写。
- 附带:`Cargo.lock`(cargo build 生成,binary crate 应入库;未加入 .gitignore)。

## 实现摘要
- 严格按 design D2 的 crate 结构起步,但 T0 只落 `main.rs` 一个文件(其余模块归 T1–T6 各自 owner,守"一文件一 owner")。
- 依赖按 D1 全部列入并钉**主版本**:clap(derive)/serde/toml/reqwest(blocking+rustls-tls,关 default-features)/keyring/arboard/directories/anyhow/thiserror/zeroize。reqwest 显式 `default-features = false` 以走 rustls 而非 native-tls(D1 要求 rustls)。
- clap 子命令对应 spec S4:`list` / `show` / `env` / `code` / `key {set,check}` / `refresh`;`env`/`code` 带 `--profile`、`--copy`,`key set/check` 取 `<id[#profile]>` 位置参数。
- 关键取舍:命令体一律 `todo!()`,因 clap 在 `parse()` 阶段就处理 `--help`/`--version` 并提前退出,空壳也能完整列出子命令,不影响 DoD;业务逻辑全部留待后续任务,避免越界。
- 范围守门:**未**注册 `run --`(D7 预留)、未引入任何 Linux/Vault/GUI/bws 相关物;依赖里无 `bws`、无 async 运行时。

## 自测
- `cargo build` → `Finished dev profile ... in 56.11s`,无 error / 无 warning。
- `cargo clippy` → `Finished`,无 error / 无 warning。
- `qiao --help` → 列出全部子命令(list/show/env/code/key/refresh + help)。
- `qiao key --help` → 列出 `set` / `check` 两个子命令。
- `grep` 确认 `.gitignore` 含 `.env`、`*.log`、`/target/`。
- 环境说明:本机原无 Rust 工具链,经维护者手动安装 rustup(cargo 1.96.0);CI/evaluator 侧需先有 stable 工具链。

## 对照 DoD
- [x] `cargo build` 通过 —— 见自测,无 error/warning。
- [x] `qiao --help` 列出全部子命令 —— 见自测输出,S4 六条命令齐(key 含 set/check)。
- [x] `.gitignore` 含 `target/`、`.env`、`*.log` —— `grep` 确认(`/target/`、`.env`/`.env.*`、`*.log`)。

## 偏离/疑问
- 无 spec 偏离。两点请 evaluator 留意:
  1. `.gitignore` 沿用仓库已存在版本(满足 DoD 即未重写),其 `/target/` 写法等价于 `target/`。
  2. 生成的 `Cargo.lock` 未被忽略(binary crate 惯例入库);若评审认为应处理可在 review 指出。
- 依赖虽在 T0 即全部列入(D1 要求),但 T0 代码尚未 `use` 它们;Rust 2021 允许声明未使用依赖,build/clippy 均无 warning。后续任务直接取用。
