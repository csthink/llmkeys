# T3 状态报告

- 任务: T3 — 目录三层合并
- 状态: done
- commit: 15cadfa

## 改动文件
- `src/catalog/mod.rs`(新建)—— 字段级合并 `merge()` + `load_merged()`(三层入口)+ 4 测试。
- `src/catalog/snapshot.rs`(新建)—— 编译期嵌入快照层 + 1 测试。
- `src/catalog/overrides.rs`(新建)—— 读 `~/.config/qiao/providers.toml`,缺文件=空层 + 3 测试。
- `src/catalog/modelsdev.rs`(新建)—— models.dev 拉取 + JSON 缓存 + TTL + 失败降级 + 5 测试。
- `Cargo.toml`(改)—— 加 `serde_json`(读写 JSON 缓存;已随 reqwest json 特性在依赖树中)。
- `src/main.rs`(改)—— `mod catalog`(allow(dead_code),由 T6 接线)。

## 实现摘要
- **合并**:`merge(layers)` 按低→高字段级合并;高层非空字段覆盖低层,其余保留;`models` 逐角色合并。`load_merged()` = 快照 < models.dev(只 enrich 已知 id)< overrides。
- **降级(S6)**:`refresh()` 先 `fetch()` 成功才 `write_cache`(原子 rename),失败直接 Err、旧缓存不动;`load_layer()` 缺缓存/损坏 → 空层(list 退回快照)。任何路径不碰 key。
- **TTL**:缓存存 `fetched_at`,`status()` 返回 Missing/Fresh/Stale(默认 24h),供 T6 提示数据来源。
- **测试可控**:`persist_fetched(path, fetch_fn, now)` 把网络拉取抽成参数,降级行为不依赖真实网络即可单测;路径全部走临时文件。

## 自测
```
cargo build  → Finished,无 error/warning
cargo test   → 38 passed; 0 failed(T3 新增 13)
cargo clippy → Finished,无 error/warning
```
外部接口 models.dev/api.json 已 curl 验证:HTTP 200、138 家、每家含 name/api/env/models。**未**在测试中打真实网络(保持 hermetic)。未涉及任何真实 key。

## 对照 DoD
- [x] 无网络时 `list` 仍可用(走快照)—— `snapshot_only_yields_full_offline_catalog` + `load_layer` 缺缓存返回空层。
- [x] overrides 能覆盖快照单个字段 —— `override_replaces_single_field_only`。
- [x] `refresh` 失败保留旧缓存 —— `failed_refresh_keeps_old_cache`。
- [x] 快照预置 4 家、国内两家补全 base_url/命名 —— 沿用既有快照(T0/D5),未改动。

## 偏离/疑问(⚠️ 需维护者裁断,见变更提案 PROPOSAL-001)
1. **models.dev 字段贡献被收窄为只取 `display_name`**。原因:spec S3 合并序让 modelsdev > snapshot,而 models.dev 的 `api` 对国内是国际端点(siliconflow `.com`、deepseek 缺 `/v1`),覆盖快照会破坏国内配置,直接违反 CLAUDE.md 核心约定「国内 provider 以快照为准,不要等上游」。**裁断**:CLAUDE.md(项目宪法)优先于 spec 通用合并序,故 models.dev v1 不贡献 base_url/模型选择。这是 spec 与宪法的真实张力,已写成 `docs/proposals/PROPOSAL-001-modelsdev-scope.md`,请拍板。
2. **models.dev 层只 enrich 已知 provider**(快照∪overrides),不引入上游 138 家裸条目,否则 `list` 会列出大量无 key_ref/env_prefix 的不可用条目。若你希望 `list` 展示全部上游 provider,请在提案里指明。
3. 加了 `serde_json` 依赖(D1 未列,但为 JSON 缓存所必需,且已在 reqwest 依赖树中);为 `main.rs` 增 `mod catalog` 属必要接线。
